document.getElementById('personalidad-form').addEventListener('submit', function(event) {
    event.preventDefault(); // Esto evita que la página se recargue
    alert('Formulario enviado, pero los datos no se almacenarán todavía.');
});

